{
  "name": "groovemechanic",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm run setup:client && npm run setup:server",
    "setup:client": "npm --prefix client run prod:setup",
    "setup:server": "npm --prefix server run prod:setup",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
